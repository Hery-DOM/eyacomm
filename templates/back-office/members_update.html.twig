{% extends 'back-office/base/base.html.twig' %}

{% block content %}
    <a href="{{ path('admin_members') }}">Retour</a>
    {% for message in app.flashes('info') %}
        {{ message }}
    {% endfor %}
{{ form(form) }}

{% endblock %}